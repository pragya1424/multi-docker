sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t vishalcool4/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true vishalcool4/react-test npm test

after_success:
  - docker build -t vishalcool4/multi-client ./client
  - docker build -t vishalcool4/multi-nginx ./nginx
  - docker build -t vishalcool4/multi-server ./server
  - docker build -t vishalcool4/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push vishalcool4/multi-client
  - docker push vishalcool4/multi-nginx
  - docker push vishalcool4/multi-server
  - docker push vishalcool4/multi-worker
